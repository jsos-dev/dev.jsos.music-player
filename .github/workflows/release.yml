name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4

      - run: npm ci

      - run: npm run build

      - id: version
        run: echo "version=$(node -p 'require("./package.json").version')" >> $GITHUB_OUTPUT

      - run: |
          node -e 'const j=require("./package.json");j.devDependencies={};fs.writeFileSync("package.json",JSON.stringify(j,null,2))'

      - run: zip -r dev.jsos.music-player-${{ steps.version.outputs.version }}.zip dist/ icon.svg server.js package.json README.md

      - uses: softprops/action-gh-release@v2
        with:
          files: '*.zip'
          generate_release_notes: true
